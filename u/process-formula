#!/usr/bin/env raku
use Chomper::Core::Formula;

sub MAIN {
    my $in = $*IN.slurp;
    my $ws = FormulaPNG::Workspace.new(
        formula => $in.chomp.trim
    );
    $ws.run();
    say $in.chomp;
}
